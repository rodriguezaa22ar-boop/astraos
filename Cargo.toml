[package]
name = "astra"
version = "0.1.0"
edition = "2021"
description = "AstraOS command center for macOS developer workstations"
license = "MIT"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
