[project]
name = "polyglot-fixture"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
polyglot-fixture = "app:main"

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
